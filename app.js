const $=s=>document.querySelector(s);const cats=['كل المشاريع',...new Set(PROJECTS.map(p=>p.category))];const iconNames=['brain-circuit','landmark','life-buoy','sprout','shopping-bag','route','bar-chart-3','database'];const hints=['ذكاء • حماية','حج • سفر','مفقود • طوارئ','زراعة • بيئة','تجارة • مزادات','نقل • خدمات','أعمال • تحليلات','بيانات • برمجيات'];const grid=$('#grid'),filters=$('#filters'),search=$('#search'),count=$('#count'),empty=$('#empty');let active='كل المشاريع';
function categoryCards(){ $('#categoryGrid').innerHTML=cats.slice(1).map((c,i)=>`<button class="category-card" data-cat="${c}"><span class="cat-num">${String(i+1).padStart(2,'0')}</span><span class="cat-icon"><i data-lucide="${iconNames[i%iconNames.length]}"></i></span><h3>${c}</h3><span>${hints[i]||'Technology'}</span></button>`).join('');document.querySelectorAll('.category-card').forEach(x=>x.onclick=()=>{active=x.dataset.cat;renderFilters();render();$('#projects').scrollIntoView({behavior:'smooth'})});if(window.lucide)lucide.createIcons()}
function renderFilters(){filters.innerHTML=cats.map(c=>`<button class="filter ${c===active?'active':''}" data-cat="${c}">${c}</button>`).join('');document.querySelectorAll('.filter').forEach(x=>x.onclick=()=>{active=x.dataset.cat;renderFilters();render()})}
function list(){const q=search.value.trim().toLowerCase();return PROJECTS.filter(p=>(active==='كل المشاريع'||p.category===active)&&(!q||`${p.title} ${p.description} ${p.category}`.toLowerCase().includes(q)))}
function render(){const data=list();count.textContent=`${data.length} مشروع`;empty.classList.toggle('hidden',data.length>0);grid.innerHTML=data.map((p,i)=>`<article class="project-card" data-id="${p.id}" style="animation-delay:${Math.min(i*18,240)}ms"><div class="card-top"><span class="badge">${p.category}</span><span class="project-id">#${String(p.id).padStart(2,'0')}</span></div><h3>${p.title}</h3><p>${p.description}</p><div class="card-bottom"><span class="details">عرض التفاصيل</span><span class="arrow"><i data-lucide="arrow-left"></i></span></div></article>`).join('');document.querySelectorAll('.project-card').forEach(x=>x.onclick=()=>location.href=`project.html?id=${x.dataset.id}`);if(window.lucide)lucide.createIcons()}
search.oninput=render;$('#heroSearch').oninput=e=>{search.value=e.target.value;active='كل المشاريع';renderFilters();render()};$('#heroSearchBtn').onclick=()=>{$('#projects').scrollIntoView({behavior:'smooth'});setTimeout(()=>search.focus(),250)};$('#clear').onclick=()=>{active='كل المشاريع';search.value='';$('#heroSearch').value='';renderFilters();render()};
const mobileNav=document.querySelector('.desktop-nav');$('#menu').onclick=()=>{const open=mobileNav.classList.toggle('mobile-open');Object.assign(mobileNav.style,open?{display:'flex',position:'absolute',top:'65px',right:'14px',left:'14px',padding:'9px 14px',background:'#fff',border:'1px solid #e4e9e7',borderRadius:'10px',boxShadow:'0 15px 35px rgba(20,35,50,.08)',flexDirection:'column',alignItems:'stretch',gap:'0'}:{display:'none'});if(open)mobileNav.querySelectorAll('a').forEach(a=>a.onclick=()=>{mobileNav.classList.remove('mobile-open');mobileNav.style.display='none'})};categoryCards();renderFilters();render();